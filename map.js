const numbers=[12,23,45];//Primary Goal Transform elements
const numbers1= numbers.map(num=> num * 2);//Transform each element and returns new array ,doesn't change original array
console.log(numbers1);//Output Array Length Same as original
